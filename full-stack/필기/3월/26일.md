# 3월 27일 필기 — screen-implement (화면 구현)

## 핵심요약

Swiper는 `new Swiper(선택자, 옵션)`으로 인스턴스를 생성하며, loop·autoplay·pagination·navigation 옵션으로 슬라이더를 제어한다. 인스턴스를 2개 만들어 수평 메인 배너와 수직 공지 롤링을 동시에 독립적으로 운영할 수 있다. 멀티 슬라이드는 slidesPerView·centeredSlides로 여러 슬라이드를 한 화면에 표시하며, mouseover/mouseleave로 autoplay를 제어하고 slideToLoop으로 클릭한 슬라이드로 이동한다. Lodash `_.throttle`로 scroll 이벤트의 콜백 실행 빈도를 제한하고, ScrollMagic으로 특정 스크롤 지점에서 클래스를 토글해 CSS 효과를 발동시킨다.


### 예제 파일

| 경로 | 다루는 기능(요지) |
|------|-------------------|
| 단일 배너 예제 | 단일 `Swiper` 인스턴스, `pagination`·`navigation`·`autoplay` API |
| 이중 인스턴스 예제 | 인스턴스 2개(수직·수평), 동일 배너 옵션 + `autoplay` 토글 |
| 멀티 슬라이드 예제 | `slidesPerView`·`centeredSlides`·`slideToLoop`, 호버 시 `autoplay` 제어 |
| 01SWIPER/images/ | 슬라이드 이미지 리소스 (`1.jpg` ~ `4.jpg`) |
| Lodash throttle 예제 | `_.throttle`로 `scroll` 콜백 실행 빈도 제한 |
| ScrollMagic 기초 예제 | `ScrollMagic.Scene`·`setClassToggle`·`Controller`로 스크롤 트리거 |

### 예제 파일별 추가 메모
- 단일 배너 예제: Swiper를 처음 붙일 때 필요한 최소 구조인 `.swiper`, `.swiper-wrapper`, `.swiper-slide` 조합 확인용. 옵션보다 먼저 마크업 뼈대 파악용.
- 이중 인스턴스 예제: 수평 배너와 수직 공지처럼 서로 다른 역할의 슬라이더를 한 화면에 동시에 둘 수 있다는 점 확인용. 인스턴스별 독립 제어 감각 익히기용.
- 멀티 슬라이드 예제: 여러 장을 한 번에 보여 주면서 가운데 항목에 시선을 모으는 패턴 정리용. 단순 자동재생보다 hover 정지와 클릭 이동 같은 사용자 조작 대응 확인용.
- 슬라이드 이미지 리소스: 슬라이더 데모가 실제 리소스를 어떻게 참조하는지 확인하는 용도. 경로 오류 발생 시 이미지 로딩부터 점검하기 위한 기준용.
- Lodash throttle 예제: "이벤트는 많이 오더라도 실행은 줄인다"는 제어 개념 확인용. scroll 외 resize, mousemove 같은 빈번한 이벤트 응용용.
- ScrollMagic 기초 예제: 특정 지점에서 클래스를 토글해 CSS 효과를 발동시키는 구조 확인용. 스크롤 감지와 실제 연출의 역할 분리 파악용.
  - **`new Swiper(선택자, 옵션)`** : Swiper 12 번들 CDN으로 인스턴스 생성.
  - **`direction: 'horizontal'`** , **`effect: 'slide'`** , **`loop: true`** , **`mousewheel: true`** : 전환 방향·효과·무한 루프·휠 이동.
  - **`autoplay: { delay: 2000 }`** : 자동 재생 간격(ms).
  - **`pagination`** : `el`로 페이지네이션 DOM 지정, **`type: 'fraction'`** (분수형 현재/전체), **`clickable: true`**.
  - **`navigation`** : **`prevEl`** / **`nextEl`** 로 이전·다음 버튼 요소 연결.
  - **`mainBannerSwiper.autoplay.stop()`** / **`autoplay.start()`** : 클릭 핸들러에서 자동 재생 일시정지·재개(아이콘 문자열만 교체하는 UI와 연동).
### Swiper 이중 인스턴스 예제 요약
  - **`new Swiper('.main-banner>.swiper', { … })`** : 기본 배너 예제와 동일한 배너 옵션(`fraction` 페이지네이션, `navigation`, `autoplay`, `loop`, `mousewheel` 등).
  - **`new Swiper('.top-header>.swiper', { direction: 'vertical', autoplay, loop })`** : 별도 인스턴스로 수직 자동 롤링(공지 텍스트용).
  - **`autoplay.stop` / `autoplay.start`** : 배너에 한해 재생 토글(상단 수직 Swiper는 별도 제어 없음).
### Swiper 멀티 슬라이드 예제 요약
  - 배너·상단 수직 Swiper는 앞선 이중 인스턴스 예제와 동일한 API 사용.
  - **`new Swiper('main>.multi-slider>.swiper', { … })`** : **`slidesPerView: 4`**, **`centeredSlides: true`**, **`loop`**, **`autoplay`**, **`navigation`** — 한 화면에 여러 슬라이드, 가운데 정렬.
  - **`multiSliderSwiper.autoplay.stop()`** / **`start()`** : **`mouseover`** / **`mouseleave`** 로 멀티 슬라이더에 포인터가 올라갈 때만 자동재생 끊기.
  - **`slideToLoop(realIndex)`** : 슬라이드 **`click`** 시 `data-swiper-slide-index`를 읽어 루프 모드에서 해당 실제 인덱스로 이동(가운데 슬라이드 맞추기). **`Element.closest()`** 로 클릭 대상이 슬라이드인지 판별.
### Lodash throttle 예제 요약
  - **`_.throttle(함수, 100)`** : Lodash가 반환한 함수를 `scroll` 리스너에 넣어, 스크롤 이벤트가 아무리 자주 와도 콜백은 최대 약 100ms 간격으로만 실행(연속 호출 부하 완화).
  - 콜백 안에서는 **`window.scrollY`** 로 현재 스크롤 위치를 로그.
### ScrollMagic 기본 트리거 예제 요약
  - **`new ScrollMagic.Controller()`** : 스크롤을 감시하는 제어 객체.
  - **`new ScrollMagic.Scene({ triggerElement, triggerHook })`** : **`triggerElement`** 가 뷰 기준 어느 위치에 올 때 씬이 활성화될지 정함. **`triggerHook: 0.5`** 는 뷰포트 세로 중앙(0~1 스케일).
  - **`.setClassToggle(대상요소, '클래스명')`** : 씬 진입·이탈에 맞춰 클래스 추가/제거(여기서는 `.ball`에 **`move`** 토글).
  - **`.addTo(controller)`** : 위 씬을 컨트롤러에 등록해 실제로 스크롤과 연동.

---

