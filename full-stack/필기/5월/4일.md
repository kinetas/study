# 5월 4일 필기 — JAVA

## 핵심요약

`static` 변수는 클래스(메서드) 영역에 적재되어 객체 생성과 무관하게 모든 인스턴스가 공유하며, `static` 메서드에서는 인스턴스 변수를 사용할 수 없다. 싱글톤 패턴은 `private` 생성자 + `private static` 인스턴스 필드 + `public static getInstance()` 의 세 요소로 프로그램 전체에서 단 하나의 객체만 생성되도록 보장한다. 상속은 `extends` 예약어로 상위 클래스의 속성·기능을 하위 클래스가 물려받는 문법이며, 자식 생성자는 첫 줄에서 `super()` / `super(args)` 로 부모 생성자를 반드시 호출한다. 오버라이딩은 상속을 전제로 메서드 헤더는 동일하게 두고 본문만 재정의하는 것으로 다형성의 근거가 되며, 오버로딩과 달리 상속이 필수 조건이다. 업캐스팅은 자식 객체를 부모 타입 참조변수에 자동 대입하는 것으로 항상 안전하고, 업캐스팅 상태에서 오버라이딩된 메서드는 정상 호출된다. 다운캐스팅은 강제 형변환이며 반드시 업캐스팅이 전제되어야 하고, `instanceof` 로 타입을 먼저 확인하는 것이 안전하다.

---

## JAVA02 — CH04 static & 싱글톤 패턴

---

### 1) static 변수 (클래스 변수)

`static` 으로 선언된 변수는 **클래스(메서드) 영역**에 적재 — 객체 생성과 무관하게 공간이 할당되고 모든 인스턴스가 공유한다.

| 구분 | 선언 | 저장 위치 | 공유 |
|---|---|---|---|
| 인스턴스 변수 | `int n2` | Heap (객체마다 별도) | 객체별 독립 |
| 클래스(static) 변수 | `static int n1` | 클래스(메서드) 영역 | 모든 객체 공유 |

```java
class C01Simple {
    static int n1;  // 클래스 변수 — 공유
    int n2;         // 인스턴스 변수 — 객체별 독립
}

C01Simple.n1 = 1234;        // 객체 없이 클래스명으로 접근 가능

C01Simple ob1 = new C01Simple();
C01Simple ob2 = new C01Simple();

ob1.n1 = 2233;
// ob1.n1 == 2233, ob2.n1 == 2233  ← 같은 공간 공유

ob2.n1 = 56789;
// ob1.n1 == 56789, ob2.n1 == 56789  ← 한쪽 수정이 모두 반영
```

---

### 2) static 메서드

`static` 메서드는 객체 생성 전에 메모리에 올라오므로, **인스턴스 변수/메서드를 사용할 수 없다** — `static` 변수나 지역 변수만 사용 가능.

```java
class C02Simple {
    static int n1;
    int n2;

    void func1() {
        n1 = 100;   // OK — static 변수
        n2 = 200;   // OK — 인스턴스 메서드에서 인스턴스 변수 접근 가능
    }

    static void func2() {
        n1 = 300;   // OK — static 변수
        // n2 = 400; // 컴파일 에러 — static 메서드에서 인스턴스 변수 사용 불가
    }
}
```

> `static` 메서드는 이미 적재되어 있지만, `n2` 는 아직 객체가 없어 존재하지 않기 때문

---

### 3) 싱글톤 패턴 (Singleton Pattern)

**프로그램 전체에서 단 하나의 객체만 생성**되도록 보장하는 디자인 패턴.

```
1. 생성자를 private 으로 막음 → 외부에서 new 불가
2. static 필드에 단일 인스턴스 보관
3. public static getInstance() 로만 접근 허용 → 없으면 생성, 있으면 기존 반환
```

```java
class Company {
    private static Company instance;  // 유일한 인스턴스 보관

    private Company() {}              // 외부 new 차단

    public static Company getInstance() {
        if (instance == null)
            instance = new Company(); // 최초 1회만 생성
        return instance;
    }

    int n1, n2;

    @Override
    public String toString() {
        return "Company [n1=" + n1 + ", n2=" + n2 + "]";
    }
}

Company com1 = Company.getInstance();
Company com2 = Company.getInstance();

com1.n1 = 100;
com2.n2 = 200;

System.out.println("com1 : " + com1); // Company [n1=100, n2=200]
System.out.println("com2 : " + com2); // Company [n1=100, n2=200]
// com1 == com2 → true (같은 객체)
```

---

### 4) static 메서드 예제 : ArrayUtils (C04)

두 배열을 이어 붙이는 `concat()`과 배열을 출력하는 `print()`를 static 메서드로 구현.

#### concat(int[] a, int[] b)

```
a[] = {1, 5, 7, 9}        인덱스 0~3
b[] = {3, 6, -1, 100, 77} 인덱스 0~4
                           ↓
c[] = {1, 5, 7, 9, 3, 6, -1, 100, 77}
      ← a.length →← b.length →
```

**방법 1 — for 루프 직접 복사**

```java
int[] c = new int[a.length + b.length];
for (int i = 0; i < a.length; i++) c[i] = a[i];
for (int i = 0; i < b.length; i++) c[i + a.length] = b[i];
```

**방법 2 — System.arraycopy() (최종 코드)**

```java
public static int[] concat(int[] a, int[] b) {
    int[] c = new int[a.length + b.length];
    System.arraycopy(a, 0, c, 0,        a.length); // a → c[0]부터
    System.arraycopy(b, 0, c, a.length, b.length); // b → c[a.length]부터
    return c;
}
// System.arraycopy(src, srcPos, dest, destPos, length)
```

#### print(int[] a)

```java
// 방법 1 — 향상된 for + printf
for (int el : a) System.out.printf("%d ", el);

// 방법 2 — Stream + 메서드 참조
Arrays.stream(a).forEach(System.out::println);

// 방법 3 — Stream + 람다 (최종 코드)
Arrays.stream(a).forEach(el -> System.out.println(el));
```

---

### 5) 싱글톤 패턴 연습 : ShopService (C05)

```java
class ShopService {
    private static ShopService instance;

    private ShopService() {}

    public static ShopService getInstance() {
        if (instance == null)
            instance = new ShopService();
        return instance;
    }
}

ShopService obj1 = ShopService.getInstance();
ShopService obj2 = ShopService.getInstance();

System.out.println(obj1 == obj2); // true — 같은 객체
// "같은 ShopService 객체입니다."
```

> **싱글톤 핵심 3요소** : `private` 생성자 / `private static` 인스턴스 필드 / `public static getInstance()`

---

## JAVA02 — CH05 상속 (Inheritance)

---

### 1) 상속 개념 (C00)

```
상속(Inheritance) : 상위(부모, super) 클래스의 속성과 기능을
                    하위(자식, sub) 클래스에서 그대로 받아 사용할 수 있도록 허용한 문법

- 하위 클래스는 물려받은 것 외에 속성/기능을 추가할 수 있다
- 하위 클래스는 물려받은 기능을 고쳐서 사용할 수 있다 → 오버라이딩(Overriding)
- extends 예약어 사용
```

---

### 2) 기본 상속 — Point2D → Point3D (C01)

디폴트 생성자만 있는 가장 단순한 상속 형태.

```java
class Point2D {
    int x, y;

    Point2D() { System.out.println("Point2D 디폴트생성자 호출"); }

    @Override
    public String toString() { return "Point2D [x=" + x + ", y=" + y + "]"; }
}

class Point3D extends Point2D {
    int z;

    Point3D() { System.out.println("Point3D 디폴트생성자 호출"); }

    @Override
    public String toString() { return "Point3D [z=" + z + ", x=" + x + ", y=" + y + "]"; }
}

Point3D ob = new Point3D();
ob.x = 10;  ob.y = 20;  ob.z = 30;
System.out.println(ob);
// Point2D 디폴트생성자 호출   ← 부모 생성자 먼저 실행
// Point3D 디폴트생성자 호출
// Point3D [z=30, x=10, y=20]
```

---

### 3) 상속 + super() 생성자 호출 (C02)

`super()` / `super(args)` 로 부모 생성자를 명시적으로 호출.  
생성자 첫 줄에 없으면 컴파일러가 `super()` 를 자동 삽입.

```java
class Point3D extends Point2D {
    int z;

    Point3D() {
        super();   // Point2D() 호출
        System.out.println("Point3D 디폴트생성자 호출");
    }

    Point3D(int x) {
        // super.x = x;  // 이렇게도 가능하지만
        // this.x = x;   // 이것도 가능하나
        super(x);         // 부모 생성자에게 위임하는 것이 올바른 방법
        System.out.println("Point3D(int x) 생성자 호출");
    }

    Point3D(int x, int y)       { super(x, y);    System.out.println("Point3D(int x, int y) 생성자 호출"); }
    Point3D(int x, int y, int z) { super(x, y); this.z = z; System.out.println("Point3D(int x, int y, int z) 생성자 호출"); }
}

Point3D ob = new Point3D(10, 20, 30);
System.out.println(ob);
// Point2D(int x, int y) 생성자 호출   ← super(x,y) 로 부모 먼저
// Point3D(int x, int y, int z) 생성자 호출
// Point3D [z=30, x=10, y=20]
```

---

### 4) 메서드 오버라이딩 (C03)

상속 관계를 전제로 **상위 클래스의 메서드를 하위 클래스가 재정의** — 헤더는 동일, 본문(`{}`)만 수정.

#### 오버로딩 vs 오버라이딩

| 구분 | 오버로딩 | 오버라이딩 |
|---|---|---|
| 상속 전제 | X | O |
| 함수 헤더 변경 | O (파라미터 변동, 함수명 고정) | X (헤더 동일) |
| 함수 본문 변경 | 무관 | O |
| 목적 | 개발 편의성 | 다형성 |

```java
class Animal { void sound() { System.out.println("소리낸다"); } }
class Dog extends Animal { void sound() { System.out.println("dog"); } }
class Cat extends Animal { @Override void sound() { System.out.println("cat"); } }

Dog d = new Dog();
d.sound();              // dog

Animal d2 = new Dog();  // 업캐스팅
d2.sound();             // dog ← 오버라이딩된 메서드 호출 (다형성)
```

---

### 5) 오버로딩 + 오버라이딩 혼합 (C04)

```java
class Super {
    int sum(int x, int y)        { return x + y; }
    int sum(int x, int y, int z) { return x + y + z; }  // 오버로딩
}

class Sub extends Super {
    int sum(int x, int y)                 { return x + y + 1; }         // 오버라이딩
    int sum(int x, int y, int z)          { return x+x + y+y + z+z; }  // 오버라이딩
    int sum(int x, int y, int z, int h)   { return x + y + z + h; }    // 오버로딩
    int sum(int x, double y)              { return (int)(x + y); }      // 오버로딩
    double sum(int x, double y, double z) { return x + y + z; }        // 오버로딩
}
```

---

### 6) 연습 문제 — 이것이자바다 (C05)

**1번 — 자바 상속 중 틀린 것**
- **답 : ①** 자바는 다중 상속을 허용한다 → 자바는 다중 상속 **불가**

**2번 — 클래스 타입 변환 중 틀린 것**
- **답 : ②** 부모 객체는 어떤 자식 타입으로도 강제 타입 변환된다 → 업캐스팅이 전제된 경우에만 가능

**3번 — final 키워드 중 틀린 것**
- **답 : ①** final 클래스는 부모 클래스로 사용할 수 있다 → final 클래스는 상속 **불가**

**4번 — 오버라이딩 중 틀린 것**
- **답 : ④** protected 메소드는 다른 패키지 자식에서 재정의 불가 → `protected`는 다른 패키지 자식도 접근 **가능**

**5번 — 추상 클래스 중 틀린 것**
- **답 : ②** 추상 메소드를 반드시 가져야 한다 → 추상 클래스에 추상 메소드 **없어도 됨**

**6번 — Child 생성자 컴파일 에러 이유 및 해결**
```java
// 오류 코드
public Child(String name, int studentNo) {
    this.name = name;       // 부모 생성자를 호출하지 않음
    this.studentNo = studentNo;
}
// 수정
public Child(String name, int studentNo) {
    super(name);            // 부모 생성자 위임
    this.studentNo = studentNo;
}
```

**7번 — 생성자 호출 순서 출력 결과**
```java
class Parent {
    public Parent()              { this("대한민국"); System.out.println("Parent() call"); }
    public Parent(String nation) { this.nation = nation; System.out.println("Parent(String nation) call"); }
}
class Child extends Parent {
    public Child()            { this("홍길동"); System.out.println("Child() call"); }
    public Child(String name) { this.name = name; System.out.println("Child(String name) call"); }
}
// new Child() 출력 결과 :
// Parent(String nation) call
// Parent() call
// Child(String name) call
// Child() call
```

**8번 — SnowTire 업캐스팅 + 오버라이딩**
```java
SnowTire snowTire = new SnowTire();
Tire tire = snowTire;   // 업캐스팅

snowTire.run(); // 스노우 타이어가 굴러갑니다.
tire.run();     // 스노우 타이어가 굴러갑니다. ← 업캐스팅 후에도 오버라이딩 메서드 호출
```

**9번 — 빈칸에 들어올 수 없는 코드**
- **답 : ②** `(B) new A()` — 업캐스팅 전제 없는 강제 변환은 불가

**10번 — Computer 추상 메서드 오류**
- **답** : `work()` 추상 메서드를 오버라이딩하지 않아서 오류. 재정의하거나 `abstract class Computer` 로 선언

**11번 — super 로 부모 메서드 호출**
```java
public void onCreate() {
    super.onCreate();   // 부모 메서드 호출
    System.out.println("추가적인 실행 내용");
}
```

**12번 — instanceof 로 타입 확인**
```java
if (a instanceof C c) {  // Java 16+ 패턴 매칭
    c.method2();
}
```

---

### 7) 연습 문제 — 명품자바 5장 (C05)

**[2번]** 자바의 모든 클래스가 반드시 상속받는 클래스 → **Object**

**[3번] 상속으로 중복 제거**
```java
class Pen {
    private int amount;
    public int getAmount()            { return amount; }
    public void setAmount(int amount) { this.amount = amount; }
}
class SharpPen extends Pen    { private int width; }
class BallPen extends Pen     { private String color;
    public String getColor()          { return color; }
    public void setColor(String c)    { this.color = c; }
}
class FountainPen extends BallPen {
    public void refill(int n) { setAmount(n); }
}
```

**[4번]** 빈칸 : `서브 클래스`, `extends`, `super`, `instanceof`, `interface`

**[5번]** protected 멤버는 같은 패키지 + 다른 패키지 서브 클래스 모두 접근 가능 → **2번 오류**

**[6번]** ColorTV 생성자 빈칸 → `super(size);`

**[7번]** `new B(11)` 출력 결과
```
A        ← B(int x) → 암묵적 super() → A()
B: 11
```

**[8번]** A에 기본 생성자 없어서 B의 `super()` 호출 실패 → `public A(){}` 추가하거나 `B(){ super(1); b=0; }` 수정

**[9번] 추상 클래스 오류**
```
(1) void f(); → abstract void f(); 로 수정
(2) 오류 없음 — 추상 클래스에 추상 메소드 없어도 됨
(3) C가 f() 재정의 안 함 → abstract class C extends B 로 수정
(4) 리턴 타입 불일치 → int f() { return 0; } 으로 수정
```

**[10번] OddDetector 구현**
```java
@Override
public boolean isOdd() { return n % 2 != 0; }
// new B(10).isOdd() → false
```

**[11번] 업캐스팅 / instanceof**
```
(1) 업캐스팅 : B b = new C()  /  A a = new D()
(2) x instanceof B → true  /  x instanceof C → false
(3) new D() instanceof Object → true  /  "Java" instanceof Object → true
```

**[12번] 동적 바인딩**
```java
Shape s = new Circle();
s.draw();                        // Circle (오버라이딩 메서드 호출)
// paint() 에서 "Circle" → draw();
// paint() 에서 "Shape"  → super.draw();
```

**[13번] 추상 클래스 + draw() 재정의**
```java
public void draw() { System.out.println("반지름=" + radius); }
// Shape s = new Shape(); → 오류 (추상 클래스 객체 생성 불가)
```

**[14번]** 다형성은 모호한 문제를 일으킨다 → **4번 오류** (다형성은 자바의 핵심 특성)

**[15번]** 인터페이스는 멤버 변수(인스턴스 필드) 선언 불가 → **2번 오류**

**[16번] interface Device + TV/ColorTV 구현**
```java
interface Device { void on(); void off(); }

class TV implements Device {
    public void on()    { System.out.println("켜졌습니다."); }
    public void off()   { System.out.println("종료합니다."); }
    public void watch() { System.out.println("방송중입니다."); }
}
```

실제 코드 — TV → ColorTV 상속:

```java
class TV {
    String brand;  int year;  int size;
    public TV(String brand, int year, int size) {
        this.brand = brand;  this.year = year;  this.size = size;
    }
    void show() { System.out.printf("%s에서 만든 %d년형 %d인치 TV", brand, year, size); }
}

class ColorTV extends TV {
    String color;
    public ColorTV(String brand, int year, int size, String color) {
        super(brand, year, size);
        this.color = color;
    }
    @Override
    void show() { System.out.printf("%s에서 만든 %d년형 %d인치 %sTV", brand, year, size, color); }
}

ColorTV myTV = new ColorTV("삼성", 2017, 32, "검정");
myTV.show(); // 삼성에서 만든 2017년형 32인치 검정TV
```

---

### 8) 업캐스팅 / 다운캐스팅 (C06)

```java
class Super { int n1; }
class Sub extends Super { int n2; }
```

#### NoCasting — 형변환 없음

```java
Super ob1 = new Super();  ob1.n1 = 10;
Sub   ob2 = new Sub();    ob2.n1 = 10;  ob2.n2 = 20;
```

#### UpCasting ★★★★★ — 자동 형변환

상위클래스형 참조변수 = 하위클래스형 객체.

이유:
1. 상속 관계의 모든 하위 객체를 하나의 타입으로 연결 가능
2. UpCasting 상태에서도 **오버라이딩된 메서드** 접근 가능

```java
Super ob3 = new Sub();   // UpCasting
ob3.n1 = 10;
// ob3.n2 = 20;  // 접근 불가 — Super 타입이라 Sub 멤버 숨겨짐

Super ob4 = ob2;         // UpCasting
ob4.n1 = 10;
// ob4.n2 = 20;  // 접근 불가
```

#### DownCasting ★★★★ — 강제 형변환

UpCasting이 전제되어야 안전하다.

```java
// 위험한 DownCasting — UpCasting 전제 없음 → ClassCastException
// Sub ob5 = (Sub) new Super();
// ob5.n2 = 200;  // 실제 공간이 없음

// 안전한 DownCasting — ob4 는 원래 Sub 객체를 업캐스팅한 것
Sub down = (Sub) ob4;
down.n1 = 10;
down.n2 = 20;  // Sub 멤버 접근 가능
```

| 구분 | 방향 | 변환 방식 | 안전성 |
|---|---|---|---|
| UpCasting | 자식 → 부모 타입 | 자동 | 항상 안전 |
| DownCasting | 부모 → 자식 타입 | 강제 `(SubType)` | UpCasting 전제 필요 |
